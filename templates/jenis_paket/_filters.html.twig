{{ form_start(filter) }}
    <tr>
        <td>{{ form_widget(filter.no) }}
        <td>{{ form_widget(filter.nama_jenis_paket) }}</td>
        <td>
        <button type="submit" name="submit" class="btn btn-outline-success btn-sm">
            <i class="fa fa-search"></i> <span>{{ 'filter'|trans }}</span>
        </button>
        <a href="{{path('app_jenis_paket_index', {'_reset' : true })}}" class="btn btn-sm btn-outline-secondary">
            <i class="fa fa-recycle"></i> <span>{{ 'reset'|trans }}</span>
        </a>
    </td>
</tr>
{{ form_end(filter) }}